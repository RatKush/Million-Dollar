
# from kde_help import classify_cycle, plotted, compute_stats, plot_main_kde


# # ----------- Cycle classification -------------
# S3 = calculate_str(out_curve_df, get_ratio("S3"))
# S3 = remove_outliers(S3, lower_quantile=0.01, upper_quantile=0.99)
# hike_cycle, ease_cycle, side_ways = classify_cycle(series, S3, 50, -50)
# # fig2 = plotted(ease_cycle, series, plot_flags_cycle, PC_LINE, val_line, local_win, local_std,
# #                    "ease cycle", f"{Comdty}{str_name}({str_number}) in ease cycle- {len(ease_cycle)} pts")
# # fig3 = plotted(side_ways, series, plot_flags_cycle, PC_LINE, val_line, local_win, local_std,
# #                   "side ways", f"{Comdty}{str_name}({str_number}) in side ways- {len(side_ways)} pts")


    

    